# assets

This design doesn't currently use any raster images — the hero visual and
architecture diagram are hand-drawn inline SVG (see
`src/components/diagrams/`). Drop `hero.png`, `react.svg`, `vite.svg`, or
any other static assets in here and reference them with a relative import,
e.g. `import heroImg from "../assets/hero.png"`.
